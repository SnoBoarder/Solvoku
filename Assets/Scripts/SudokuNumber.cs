using UnityEngine;
using System.Collections;

public class SudokuNumber : MonoBehaviour
{
    private static Color TEXT_ANSWER_COLOR = new Color(242, 133, 48, 255);
    private static Color TEXT_SETUP_COLOR = Color.white;

    // editor reference
    public TextMesh _text;

	// Use this for initialization
	void Start ()
    {
	    // setup sudoku numbers

	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
